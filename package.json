{
  "name": "nerdearla-live-captions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experimento de transcripción de audio en vivo para Nerdearla 2026",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test",
    "transcribe": "node --env-file-if-exists=.env scripts/transcribe.js",
    "check:live": "node --env-file-if-exists=.env scripts/check-live.js",
    "prepare:translation": "node scripts/prepare-translation.js"
  },
  "license": "MIT",
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "qrcode": "1.5.4",
    "ws": "8.21.3"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
